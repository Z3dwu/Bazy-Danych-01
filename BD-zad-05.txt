ZAD1
	CREATE TABLE `CLIENT` AS SELECT * FROM company_2025.`CLIENT`;
	CREATE TABLE `ORDER` AS SELECT * FROM company_2025.`ORDER`;
	CREATE TABLE `EMPLOYEE` AS SELECT * FROM company_2025.`EMPLOYEE`;
	
	SELECT * FROM `ORDER`;
	
	SELECT * FROM `ORDER` WHERE ORDER_STATUS = 5;
	
	SELECT `ORDER_ID`, `ORDER_STATUS` FROM `ORDER` WHERE CLIENT_ID = 1 OR CLIENT_ID = 3 OR CLIENT_ID = 5;
	
ZAD2
	SELECT `FIRST_NAME`,`LAST_NAME` FROM EMPLOYEE WHERE FIRST_NAME = "Agnieszka" AND pensja = 3900;
	
	SELECT `FIRST_NAME`,`LAST_NAME` FROM EMPLOYEE WHERE pensja BETWEEN 5000 AND 7000;
	
	SELECT `FULL_NAME` FROM CLIENT WHERE SHORT_NAME LIKE "%eco%" AND IS_COMPANY = 1;
	
ZAD3
	SELECT `ORDER_NUMBER` FROM `ORDER` WHERE ORDER_DATE LIKE "%-06-%" OR ORDER_DATE LIKE "%-07-%";
	
	SELECT `FULL_NAME` FROM CLIENT WHERE TAX_IDENTIFIER IS NOT NULL ORDER BY SHORT_NAME;
	
	SELECT `FIRST_NAME`,`LAST_NAME` FROM EMPLOYEE ORDER BY BIRTH_DATE LIMIT 5;
	
ZAD4
	SELECT DISTINCT `FIRST_NAME` FROM EMPLOYEE;
	
	SELECT CONCAT(EMPLOYEE.`FIRST_NAME`, " - ", EMPLOYEE.`LAST_NAME`) AS "imie i nazwisko" FROM EMPLOYEE WHERE LAST_NAME LIKE "Ko%";
	
	SELECT `LAST_NAME`, `pensja` FROM EMPLOYEE WHERE DATE_OF_EMPLOYMENT BETWEEN '2010-01-01' AND '2014-01-01';
	
ZAD5
	SELECT `pensja` AS "pensja", `pensja`*0.75 AS "pensja netto", `pensja`*0.25 AS "podatek" FROM EMPLOYEE;
	
	SELECT `FULL_NAME` FROM CLIENT WHERE TAX_IDENTIFIER IS NULL;
	
	SELECT `SHORT_NAME` FROM CLIENT WHERE SHORT_NAME LIKE "K%" OR SHORT_NAME LIKE "%ski" ORDER BY SHORT_NAME DESC;